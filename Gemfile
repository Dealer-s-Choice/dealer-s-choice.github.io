source "https://rubygems.org"

# Tracks the exact gem versions GitHub Pages uses in production.
# See https://pages.github.com/versions/ for the version pinned by `github-pages`.
gem "github-pages", group: :jekyll_plugins
